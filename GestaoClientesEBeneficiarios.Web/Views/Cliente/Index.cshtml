@{
    ViewBag.Title = "Clientes";
}

<h2>@Html.Raw(ViewBag.Title)</h2>

<link rel="stylesheet" type="text/css" href="../Content/ListaCliente.css" />

<div class="clearfix">
    @Html.ActionLink("Novo Cliente", "Incluir", "Cliente", new { }, new { @class = "btn btn-sm btn-primary pull-right"})
</div>

@Html.Partial("_ListaClientes")

@section scripts {
    <script>
        const urlClienteList = '@Url.Action("ListaClientes", "Cliente", new { area = "" })';
    </script>

    @Scripts.Render("~/bundles/ListaClientes")
}